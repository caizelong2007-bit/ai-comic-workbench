# Vendor Libraries

- `tagify.min.js` and `tagify.css`: Tagify 4.35.1, downloaded from `https://unpkg.com/@yaireo/tagify@4.35.1/dist/`.

These files are vendored so the local workbench can run without a CDN or build step.
